# Imagen base con Node 20 (compatible con Wrangler/Miniflare)
FROM node:20

# Directorio de trabajo
WORKDIR /app

# Copiamos package.json primero para cachear dependencias
COPY package*.json ./

# Instalamos dependencias
RUN npm install

# Copiamos el resto del proyecto
COPY . .

# Exponemos puerto de Miniflare
EXPOSE 8787

# Comando por defecto: iniciar Miniflare en modo servidor
CMD ["npx", "miniflare", "--modules", "--assets", "public", "--port", "8787"]
